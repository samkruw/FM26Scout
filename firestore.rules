rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Players collection: read public, write open (add auth later if needed)
    match /players/{playerId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
